<nav>
    <% if(user) { %>
        <%= user.name %> | 
    <% } %>

    <% if(routes) { %>
        <% routes.forEach((route) => { %>

            <a href="<%= route.href %>"><%= route.title %></a>

        <% }); %>
    <% } %>
</nav>