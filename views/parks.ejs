<!DOCTYPE html>
<html lang="en">
<%- include( 'partials/head.ejs', { page: 'National Parks'}) %>
<body>
    <div class="container">
        <h1>National Parks</h1>
        <% let badges = db.Badge.find({}) %>
        <% for(let i=0; i < badges.length; i++){ %>
            <% const badge = badges[i] %>
                    <a href="/park/<%= i %>"><img class="badge-image" id="badge <% i %>" src="<%= db.Badge[i].avatar %>" alt="<%= db.Badge[i].name %>" /></a>
                        <h3><a href="/park/<%= i %>"><%= db.Badge.name %></a></h3>
        <%  }   %>
    </div>
</body>
</html>